# lima

lima vm yaml files.

## Command

### validate
`limactl validate alpine.yaml`

### create & start
`limactl start alpine.yaml`

### start
`limactl start alpine`

### stop
`limactl stop alpine`

### delete
`limactl delete alpine`
